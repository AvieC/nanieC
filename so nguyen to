#include <iostream>
#include <math.h>
using namespace std;
bool sngt(int n) {
    int dem = 0;
    for (int i = 2; i < n; i++) {
        if (n%i == 0) dem++;
    }
    return dem == 0;
}
int main() {
    int n;
    cin >> n;

    for (int i = 2; i <= n; i++) {
        if (sngt(i)) cout << i << ' ';
    }
    return 0;
}

